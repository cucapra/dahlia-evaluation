KERNEL           = aes
ALGORITHM_NAME   = aes
VERSION_NAME	 = baseline
HW_SRCS          = $(KERNEL).cpp
HOST_SRCS        = main.cpp local_support.cpp support.cpp
HWDIR            = $(KERNEL)_dir
DATA             = input.data check.data

include sds.mk

